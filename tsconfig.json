{
  "compilerOptions": {
    "target": "es6", // Phiên bản ECMAScript mục tiêu (ES5, ES6, ESNext, v.v.)
    "module": "commonjs", // Hệ thống module (commonjs, es6, amd, v.v.)
    "rootDir": "./ts", // Thư mục đầu ra cho các file biên dịch
    "outDir": "./node", // Thư mục nguồn chứa các file TypeScript
    "strict": true, // Kích hoạt tất cả các kiểm tra nghiêm ngặt
    "esModuleInterop": true, // Cung cấp tính tương thích với các module ES6
    "skipLibCheck": true, // Bỏ qua kiểm tra loại cho các file khai báo thư viện
    "forceConsistentCasingInFileNames": true // Bắt buộc sử dụng tên file nhất quán
  },
  "include": [
    "ts/**/*.ts" // Bao gồm tất cả các file .ts trong thư mục src
  ],
  "exclude": [
    "node_modules" // Loại trừ thư mục node_modules
  ]
}